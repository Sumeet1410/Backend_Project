version : "3.8"
services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
  worker:
    build:
      context: .
      dockerfile: Dockerfile.worker
    env_file:
      - .env